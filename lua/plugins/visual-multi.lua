return {
	"mg979/vim-visual-multi",
	-- VeryLazy: defers its large plugin/ script past startup while still
	-- registering all default mappings (<C-n>, <C-Up/Down>, \\ leader, ...)
	-- before you can type. Keys-only lazy loading is discouraged upstream.
	event = "VeryLazy",
}
